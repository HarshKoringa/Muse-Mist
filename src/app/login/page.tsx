import { Login } from "@/components/Login";

export const metadata = {
  title: "Login | Muse & Mist",
  description: "Sign in or create your Muse & Mist account",
};

export default function LoginPage() {
  return <Login />;
}
